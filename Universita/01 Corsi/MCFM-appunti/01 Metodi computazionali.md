
Riduzionismo: modelli con valenza piu possibile generale
approccio riduzionista: cercare modelli cosi

potere predittivo -> virtuale (teoricamente predico, ma nella realta no)
				->effettivo

entra in gioco il capire come percepiamo la realta
come rendere il potere predittivo virtuale -> reale?

simulazioni al computer (permettono di sfruttare il modello in maniera piu produttiva)
limiti hardware

algoritmi e calcolo ad alte prestazioni

architettura -> hardware
networking -> connessione veloce tra gli elementi (reti)

Architecture

processing power:
- cpu (array di processing cores)
- gpu (operazioni matriciali)
- tpu (google)

storage e memoria:
- taglia
- caching rate (quanto veloce la memoria scambia info con cpu)

networking:
- maggior costo di consumo energetico

algoritmi
simulazione di un sistema a N particelle -> serve che il costo della simulazione scali con N
es FFT $N^2$ -> NlogN

Software
un po di caratteristiche

gordon bell prize

Dinamica molecolare:
- non è una disciplina ma un algoritmo
- è in grado di ricostruire la traiettoria di moltissime particelle interagenti di un sistema

tecniche di campionamento avanzato:
- miglioramento perche la dinamica molecolare è lenta
